[package]
name = "infer_tokenizer"
version = "0.1.0"
edition = "2021"

[lib]
name = "infer_tokenizer"
crate-type = ["cdylib"]

[dependencies]
tokenizers = { version = "0.22", default-features = false, features = ["onig"] }
